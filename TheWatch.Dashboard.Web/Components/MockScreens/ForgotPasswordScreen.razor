
<div class="forgot-password-screen">
    <div class="forgot-header">
        <button class="back-btn" title="Go back">‹</button>
        <span class="header-title">Reset Password</span>
        <div style="width: 44px;"></div>
    </div>

    <div class="forgot-content">
        <div class="reset-card">
            <h1 class="card-title">Reset your password</h1>
            <p class="card-subtitle">Enter your registered email or phone number to receive a password reset code</p>

            <form class="reset-form">
                <div class="form-group">
                    <label for="email-phone">Email or Phone Number</label>
                    <input
                        id="email-phone"
                        type="text"
                        placeholder="Email or phone number"
                        class="form-input"
                    />
                </div>

                <button type="submit" class="btn-send">Send Reset Code</button>
            </form>

            <div class="form-links">
                <a href="#" class="form-link">Back to Login</a>
            </div>
        </div>
    </div>
</div>

@code {
}

<style scoped>
    .forgot-password-screen {
        display: flex;
        flex-direction: column;
        height: 100%;
        background: #f8f9fa;
    }

    .forgot-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 12px 16px;
        border-bottom: 1px solid #eee;
        background: #FFFFFF;
    }

    .back-btn {
        background: none;
        border: none;
        font-size: 28px;
        color: #1B2A4A;
        cursor: pointer;
        padding: 8px;
        width: 44px;
        height: 44px;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .header-title {
        font-size: 16px;
        font-weight: 600;
        color: #1B2A4A;
    }

    .forgot-content {
        flex: 1;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 24px 16px;
        overflow-y: auto;
        -webkit-overflow-scrolling: touch;
    }

    .reset-card {
        width: 100%;
        max-width: 340px;
        background: #FFFFFF;
        border-radius: 12px;
        padding: 28px 20px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    }

    .card-title {
        font-size: 22px;
        font-weight: 700;
        color: #1B2A4A;
        margin: 0 0 8px;
    }

    .card-subtitle {
        font-size: 13px;
        color: #666;
        margin: 0 0 24px;
        line-height: 1.4;
    }

    .reset-form {
        display: flex;
        flex-direction: column;
        gap: 16px;
        margin-bottom: 20px;
    }

    .form-group {
        display: flex;
        flex-direction: column;
    }

    .form-group label {
        font-size: 13px;
        font-weight: 600;
        color: #1B2A4A;
        margin-bottom: 6px;
    }

    .form-input {
        padding: 12px 14px;
        font-size: 16px;
        border: 1px solid #ddd;
        border-radius: 8px;
        background: #FFFFFF;
        color: #1B2A4A;
        transition: all 0.3s ease;
        min-height: 44px;
    }

    .form-input::placeholder {
        color: #999;
    }

    .form-input:focus {
        outline: none;
        border-color: #1B2A4A;
        box-shadow: 0 0 0 3px rgba(27, 42, 74, 0.1);
    }

    .btn-send {
        width: 100%;
        padding: 14px 16px;
        font-size: 16px;
        font-weight: 600;
        background: #1B2A4A;
        color: #FFFFFF;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        min-height: 44px;
        transition: all 0.3s ease;
    }

    .btn-send:active {
        background: #0f1729;
    }

    .form-links {
        display: flex;
        justify-content: center;
    }

    .form-link {
        font-size: 13px;
        color: #1B2A4A;
        text-decoration: none;
        font-weight: 500;
        transition: color 0.3s ease;
    }

    .form-link:active {
        color: #DC3545;
    }

    /* Scrollbar */
    .forgot-content::-webkit-scrollbar {
        width: 6px;
    }

    .forgot-content::-webkit-scrollbar-track {
        background: transparent;
    }

    .forgot-content::-webkit-scrollbar-thumb {
        background: rgba(27, 42, 74, 0.2);
        border-radius: 3px;
    }
</style>
