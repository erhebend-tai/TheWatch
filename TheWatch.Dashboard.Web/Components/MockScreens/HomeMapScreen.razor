
<div class="home-map-screen">
    <div class="map-background">
        <!-- Concentric rings -->
        <div class="ring ring-0"></div>
        <div class="ring ring-1"></div>
        <div class="ring ring-2"></div>
        <div class="ring ring-3"></div>

        <!-- User location (pulsing center) -->
        <div class="user-dot"></div>

        <!-- Responders (green dots) -->
        <div class="responder responder-1">
            <div class="dot"></div>
            <span class="label">EMT — 3 min</span>
        </div>
        <div class="responder responder-2">
            <div class="dot"></div>
            <span class="label">Fire — 5 min</span>
        </div>
        <div class="responder responder-3">
            <div class="dot"></div>
            <span class="label">Police — 4 min</span>
        </div>

        <!-- Community alerts (amber dots) -->
        <div class="alert alert-1">
            <div class="dot"></div>
            <span class="badge">!</span>
        </div>
        <div class="alert alert-2">
            <div class="dot"></div>
            <span class="badge">!</span>
        </div>

        <!-- Shelter (blue house icon) -->
        <div class="shelter">
            <span class="icon">⌂</span>
        </div>
    </div>

    <!-- Top bar overlay -->
    <div class="top-bar-overlay">
        <button class="menu-btn" title="Menu">☰</button>
        <div class="search-bar">
            <span class="search-icon">⚲</span>
            <input type="text" placeholder="Search..." class="search-input" />
        </div>
        <button class="notification-btn" title="Notifications">
            🔔
            <span class="badge">3</span>
        </button>
    </div>

    <!-- SOS Button and Status -->
    <div class="sos-section">
        <button class="sos-button">
            <span class="sos-text">SOS</span>
        </button>
        <div class="status-text">Safe — All systems normal</div>
    </div>
</div>

@code {
}

<style scoped>
    .home-map-screen {
        position: relative;
        width: 100%;
        height: 100%;
        background: #f0f0f0;
        overflow: hidden;
    }

    .map-background {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(135deg, #e8e8e8 0%, #f5f5f5 100%);
        display: flex;
        align-items: center;
        justify-content: center;
    }

    /* Concentric rings */
    .ring {
        position: absolute;
        border-radius: 50%;
        border: 2px solid;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
    }

    .ring-0 {
        width: 60px;
        height: 60px;
        border-color: rgba(220, 53, 69, 0.3);
    }

    .ring-1 {
        width: 140px;
        height: 140px;
        border-color: rgba(255, 193, 7, 0.25);
    }

    .ring-2 {
        width: 220px;
        height: 220px;
        border-color: rgba(255, 235, 59, 0.2);
    }

    .ring-3 {
        width: 320px;
        height: 320px;
        border-color: rgba(200, 200, 200, 0.15);
    }

    /* User location - pulsing dot -->
    .user-dot {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 14px;
        height: 14px;
        background: #1B2A4A;
        border-radius: 50%;
        border: 3px solid #FFFFFF;
        box-shadow: 0 0 0 4px rgba(27, 42, 74, 0.2);
        animation: pulse 2s ease-in-out infinite;
        z-index: 5;
    }

    @@keyframespulse {
        0%, 100% {
            box-shadow: 0 0 0 4px rgba(27, 42, 74, 0.2);
        }
        50% {
            box-shadow: 0 0 0 8px rgba(27, 42, 74, 0.1);
        }
    }

    /* Responders */
    .responder {
        position: absolute;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 4px;
        font-size: 10px;
    }

    .responder-1 {
        top: 25%;
        left: 35%;
    }

    .responder-2 {
        top: 40%;
        right: 20%;
    }

    .responder-3 {
        bottom: 30%;
        left: 25%;
    }

    .responder .dot {
        width: 10px;
        height: 10px;
        background: #28A745;
        border-radius: 50%;
        border: 2px solid #FFFFFF;
        box-shadow: 0 2px 4px rgba(40, 167, 69, 0.3);
    }

    .responder .label {
        font-weight: 600;
        color: #28A745;
        text-shadow: 0 1px 2px rgba(255, 255, 255, 0.8);
        white-space: nowrap;
    }

    /* Community alerts -->
    .alert {
        position: absolute;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .alert-1 {
        top: 35%;
        right: 40%;
    }

    .alert-2 {
        bottom: 45%;
        right: 30%;
    }

    .alert .dot {
        width: 10px;
        height: 10px;
        background: #FFA500;
        border-radius: 50%;
        border: 2px solid #FFFFFF;
        box-shadow: 0 2px 4px rgba(255, 165, 0, 0.3);
    }

    .alert .badge {
        position: absolute;
        width: 16px;
        height: 16px;
        background: #DC3545;
        color: #FFFFFF;
        border-radius: 50%;
        font-weight: 700;
        font-size: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        bottom: -4px;
        right: -4px;
    }

    /* Shelter -->
    .shelter {
        position: absolute;
        top: 30%;
        right: 15%;
        font-size: 28px;
        filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.2));
    }

    /* Top bar overlay */
    .top-bar-overlay {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 56px;
        background: rgba(27, 42, 74, 0.85);
        backdrop-filter: blur(10px);
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 0 12px;
        z-index: 20;
    }

    .menu-btn {
        background: none;
        border: none;
        color: #FFFFFF;
        font-size: 24px;
        cursor: pointer;
        padding: 8px;
        width: 44px;
        height: 44px;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
    }

    .search-bar {
        flex: 1;
        height: 36px;
        background: rgba(255, 255, 255, 0.15);
        border-radius: 18px;
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 0 12px;
        border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .search-icon {
        color: rgba(255, 255, 255, 0.6);
        font-size: 16px;
    }

    .search-input {
        flex: 1;
        background: none;
        border: none;
        color: #FFFFFF;
        font-size: 14px;
        outline: none;
    }

    .search-input::placeholder {
        color: rgba(255, 255, 255, 0.5);
    }

    .notification-btn {
        position: relative;
        background: none;
        border: none;
        color: #FFFFFF;
        font-size: 20px;
        cursor: pointer;
        padding: 8px;
        width: 44px;
        height: 44px;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
    }

    .notification-btn .badge {
        position: absolute;
        top: 4px;
        right: 4px;
        background: #DC3545;
        color: #FFFFFF;
        font-size: 10px;
        font-weight: 700;
        width: 18px;
        height: 18px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    /* SOS Section -->
    .sos-section {
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 12px;
        padding: 20px 16px;
        background: linear-gradient(to top, rgba(255, 255, 255, 0.95), transparent);
    }

    .sos-button {
        width: 80px;
        height: 80px;
        border-radius: 50%;
        background: #DC3545;
        color: #FFFFFF;
        border: none;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 4px 20px rgba(220, 53, 69, 0.4);
        transition: all 0.3s ease;
        position: relative;
    }

    .sos-button::before {
        content: '';
        position: absolute;
        top: -4px;
        left: -4px;
        right: -4px;
        bottom: -4px;
        border-radius: 50%;
        background: radial-gradient(circle, rgba(220, 53, 69, 0.3), transparent);
        animation: glow 2s ease-in-out infinite;
    }

    .sos-text {
        font-size: 24px;
        font-weight: 800;
        letter-spacing: 2px;
        position: relative;
        z-index: 1;
    }

    @@keyframesglow {
        0%, 100% {
            opacity: 1;
            box-shadow: 0 0 0 0 rgba(220, 53, 69, 0.4);
        }
        50% {
            opacity: 0.7;
            box-shadow: 0 0 0 8px rgba(220, 53, 69, 0);
        }
    }

    .sos-button:active {
        transform: scale(0.95);
    }

    .status-text {
        font-size: 13px;
        color: #28A745;
        font-weight: 600;
    }
</style>
